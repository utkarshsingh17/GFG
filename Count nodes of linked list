class Solution
{
    //Function to count nodes of a linked list.
    public static int getCount(Node head)
    {
        
        //Code here
        if(head==null)
        return 0;
        int c=1;
        while(head.next!=null)
        {
            c++;
            head=head.next;
        }
        return c;
    }
}
